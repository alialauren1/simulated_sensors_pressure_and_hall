**simulated sensor data from pressure and hall sensors**

Using pool data from pressure and hall effect sensors that is stored on sd card to be read and dev board will spit out the data onto i2c for the pressure values and the hall effect status on a GPIO pin. Essentially, this system will act as the two sensors, outputting on certain pins the same signals that the sensors would
